FROM node:22-alpine

WORKDIR /app

ENV NODE_ENV=production
ENV FILATRIX_API_HOST=0.0.0.0
ENV FILATRIX_API_PORT=5175
ENV FILATRIX_DATA_FILE=/data/erp-data.json

COPY server ./server
COPY shared ./shared

EXPOSE 5175
VOLUME ["/data"]

CMD ["node", "server/index.mjs"]
